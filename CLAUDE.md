# Apex Plugins

- Every time a plugin is modified, the version number of the plugin needs to be upgraded
- All plugins need to support both token-saving mode and native mode
- Use different names for scripts in different plugins to avoid hook misjudgment.
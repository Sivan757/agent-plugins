{
  "name": "apex-plugins",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --build",
    "create-plugin": "tsx scripts/create-plugin.ts"
  },
  "devDependencies": {
    "typescript": "^5.8",
    "tsx": "^4",
    "esbuild": "^0.25"
  }
}

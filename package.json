{
  "name": "apex-plugins",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "ticktick"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --build",
    "create-plugin": "tsx scripts/create-plugin.ts"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.25",
    "tsx": "^4",
    "typescript": "^5.8"
  }
}
